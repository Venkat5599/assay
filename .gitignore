node_modules/
.env
.env.*
!.env.example
out/
cache/
broadcast/
.next/
.vercel/
dist/
*.log

# Runtime output: agent decision records, regenerated by `bun run propose`.
agent/proposals.json

# Agent grade cache: regenerated, and a stale grade should never be replayed
# from a commit as though the model had just formed it.
agent/assessments.json
.playwright-mcp/

# Playwright MCP scratch output.
.playwright-mcp/

# Throwaway browser screenshots.
*.png
!frontend/public/*.png
