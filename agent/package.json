{
  "name": "lading-agent",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "bun run src/index.ts",
    "once": "bun run src/index.ts --once",
    "fund": "bun run src/fund.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.21.55"
  },
  "devDependencies": {
    "typescript": "^5.7.2"
  }
}
